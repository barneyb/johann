fn main() {
    char g = '!';
    int a = 16;
    void* a = malloc(a); # space for a pair of ints and/or pointers
    if a = null {
        printc(g);
        printc(g);
        return 1;
    }
    int b = 3;
    while true {
        b = b + 2;
        if b < 7 {
            again; # run the loop again
            printc(g);
        }
        if b > 6 {
            done; # loop is done
            printc(g);
        }
        printc(g);
    }
    free(a);
    b = itoa(b);
    println(b);
    free(b);
}
