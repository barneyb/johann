fn main() {
    char g = '!';
    int a = 16;
    # todo: use let to make a new var (with the same name)
    a = malloc(a); # space for a pair of ints and/or pointers
    if a = null {
        putchar(g);
        putchar(g);
        return 1;
    }
    int b = 3;
    while true {
        b = b + 2;
        if b < 7 {
            again; # run the loop again
            putchar(g);
        }
        if b > 6 {
            done; # loop is done
            putchar(g);
        }
        putchar(g);
    }
    free(a);
    b = itoa(b);
    println(b);
    free(b);
}
