fn main(): int { # block 0
    int f = 0;
    char c = read();
    int b = 0 - 1;
    b = c > b;
    while b { # block 1
        bool d = c = '(';
        if d { # block 2
            f = f + 1;
        } # block 2 - if
        d = c = ')';
        if d { # block 3
            f = f - 1;
        } # block 3 - if
        c = read();
        b = 0 - 1;
        b = c > b;
    } # block 1 - while
    char* a = itoa(f);
    println(a);
    return f;
} # block 0 - fn

fn goat(): int { # block 4
    char* b = "oh hai";
    return 7;
} # block 4 - fn
